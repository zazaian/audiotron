# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/audiotron.rb'

Hoe.new('audiotron', Audiotron::VERSION) do |p|
  # p.rubyforge_name = 'audiotronx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
